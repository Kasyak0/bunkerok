{
  "name": "bunker-game",
  "version": "1.0.0",
  "description": "Multiplayer survival game Bunker",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp index.html public/ && cp -r api public/"
  },
  "engines": {
    "node": "22.x"
  }
}
